workflow: GitFlow/v1
next-version: 0.1.0
branches:
  develop:
    mode: ContinuousDelivery
    prevent-increment:
      of-merged-branch: true
  feature:
    mode: ContinuousDelivery
  release:
    mode: ContinuousDelivery
  main:
    mode: ContinuousDelivery
  pull-request:
    mode: ContinuousDelivery
    label: pr
